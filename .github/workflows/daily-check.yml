name: GitHub Daily Contribution Checker

on:
  schedule:
    - cron: '0 11 * * *' # 5:00 PM Bangladesh Time (11:00 UTC)
  workflow_dispatch:

jobs:
  check-contribution:
    runs-on: ubuntu-latest
    steps:
      - name: Check GitHub push activity today
        id: check
        run: |
          # 1. Get Today's Date in BD
          bd_today=$(TZ=Asia/Dhaka date +"%Y-%m-%d")
          bd_time=$(TZ=Asia/Dhaka date +"%H:%M")
          
          # 2. Fetch events
          curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
               "https://api.github.com/users/${{ github.actor }}/events?per_page=100" > events.json

          # 3. Process events
          count=0
          timestamps=$(jq -r '.[] | select(.type=="PushEvent") | .created_at' events.json)

          for ts in $timestamps; do
            event_bd_date=$(TZ="Asia/Dhaka" date -d "$ts" +"%Y-%m-%d")

            if [ "$event_bd_date" == "$bd_today" ]; then
              count=$((count + 1))
            fi
          done

          echo "count=$count" >> $GITHUB_OUTPUT
          echo "bd_date=$bd_today" >> $GITHUB_OUTPUT
          echo "bd_time=$bd_time" >> $GITHUB_OUTPUT

      - name: Send Telegram notification
        run: |
          count="${{ steps.check.outputs.count }}"
          bd_date="${{ steps.check.outputs.bd_date }}"
          bd_time="${{ steps.check.outputs.bd_time }}"
          
          if [ "$count" -eq "0" ]; then
            message="🚨 *NO GITHUB COMMITS TODAY!*%0A%0A⏰ Time: $bd_time (BD)%0A📅 Date: $bd_date%0A%0A💪 Go commit something NOW!%0A🔥 Keep your streak alive!%0A%0A_#ProductiveVibes #CodingTime_"
          else
            message="✅ *GREAT JOB TODAY!*%0A%0A🎉 You made $count push(es) today!%0A⏰ Time: $bd_time (BD)%0A📅 Date: $bd_date%0A%0A🔥 Keep up the momentum!%0A💚 Your streak is safe!%0A%0A_#ProductiveDay #GitHubStreak_"
          fi
          
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
            -d "text=$message" \
            -d "parse_mode=Markdown"























# name: GitHub Daily Contribution Checker

# on:
#   schedule:
#     - cron: '30 17 * * *' # 11:30 PM Bangladesh Time (17:30 UTC)
#   workflow_dispatch:

# jobs:
#   check-contribution:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Check GitHub push activity today
#         id: check
#         run: |
#           # 1. Get Today's Date in BD
#           bd_today=$(TZ=Asia/Dhaka date +"%Y-%m-%d")
#           bd_time=$(TZ=Asia/Dhaka date +"%H:%M")
          
#           # 2. Fetch events
#           curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
#                "https://api.github.com/users/${{ github.actor }}/events?per_page=100" > events.json

#           # 3. Process events manually to avoid jq version errors
#           count=0
#           # Extract only the timestamps of PushEvents
#           timestamps=$(jq -r '.[] | select(.type=="PushEvent") | .created_at' events.json)

#           for ts in $timestamps; do
#             # Convert the UTC timestamp from GitHub to the actual date in Bangladesh
#             event_bd_date=$(TZ="Asia/Dhaka" date -d "$ts" +"%Y-%m-%d")

#             if [ "$event_bd_date" == "$bd_today" ]; then
#               count=$((count + 1))
#             fi
#           done

#           echo "Commits found for BD Date $bd_today: $count"
#           echo "count=$count" >> $GITHUB_OUTPUT
#           echo "bd_date=$bd_today" >> $GITHUB_OUTPUT
#           echo "bd_time=$bd_time" >> $GITHUB_OUTPUT

#       - name: Send Telegram notification
#         run: |
#           count="${{ steps.check.outputs.count }}"
#           bd_date="${{ steps.check.outputs.bd_date }}"
#           bd_time="${{ steps.check.outputs.bd_time }}"
          
#           if [ "$count" -eq "0" ]; then
#             # No commits - Send alert
#             message="🚨 *NO GITHUB COMMITS TODAY!*%0A%0A⏰ Time: $bd_time (BD)%0A📅 Date: $bd_date%0A%0A💪 Go commit something NOW!%0A🔥 Keep your streak alive!%0A%0A_#ProductiveVibes #CodingTime_"
#           else
#             # Commits found - Send success message
#             message="✅ *GREAT JOB TODAY!*%0A%0A🎉 You made $count push(es) today!%0A⏰ Time: $bd_time (BD)%0A📅 Date: $bd_date%0A%0A🔥 Keep up the momentum!%0A💚 Your streak is safe!%0A%0A_#ProductiveDay #GitHubStreak_"
#           fi
          
#           curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
#             -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
#             -d "text=$message" \
#             -d "parse_mode=Markdown"











